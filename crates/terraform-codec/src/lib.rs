//! DynamicValue codec: msgpack/JSON <-> semantic value graph <-> Rust structs.
//!
//! Planned for Phase 3. This crate will own the `DynamicValue` decode/encode
//! pipeline and the known/unknown/null handling on the wire.
